all:
  children:
    infra:
      hosts:
        gw:
        nfs:

    k8s:
      hosts:
        kind:

    k8s_cli:
      children:
        k8s:

    servers:
      children:
        infra:
        k8s:


# NOTE: check group_vars/ and union_vars/ for vars

...
