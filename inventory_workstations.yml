---

all:
  children:
    workstations:
      hosts:
        ws:
        x390yoga:
        x13yoga1:

    k8s_cli:
      children:
        workstations:


  vars:
    ansible_connection: local

    # NOTE: check group_vars/ and union_vars/ for vars

...
