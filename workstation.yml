---

- name: Apply workstation configuration
  hosts: workstations
  gather_facts: yes
  roles:
    - dotfiles
    - union_vars  # merge package_list
    - packages
    - groups
    - flatpaks
    - pip
    - cargo
    - go
    - ansible
    - gnome

...
